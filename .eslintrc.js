module.exports = {
    extends: ["react-app"],
    rules: {
      // Temporarily disable warnings for unused variables
      "no-unused-vars": "off",
      // Temporarily disable exhaustive-deps warnings
      "react-hooks/exhaustive-deps": "off"
    }
  };